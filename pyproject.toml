[project]
name = "typesafe-jev-workflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "langgraph>=1.2.11",
    "python-dotenv>=1.2.3",
    "typesafe-sdk>=0.6.0",
]
